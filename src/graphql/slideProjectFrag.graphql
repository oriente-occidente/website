fragment slideProjectFrag on ProjectRecord {
  id
  slug
  _modelApiKey
  title
  titleHero
  authors
  location
  startDate
  dates {
    startTime
    isDaily
    endTime
  }
  imageHero {
    ...imageHeroFrag
  }
}
