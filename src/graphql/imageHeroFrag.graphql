fragment imageHeroFrag on FileFieldInterface {
  id
  url
  width
  height
  responsiveImage(
    sizes: "(min-width: 1024px) 50vw, 100vw, 900px"
    imgixParams: {
      auto: [format, compress]
      ar: "10:9"
      fit: crop
    }
  ) {
    ...imgFrag
  }
}
